{
  "entities": {
    "Project": {
      "title": "Project",
      "description": "Represents a folder or project for organizing prompts.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "ownerId": { "type": "string" }
      },
      "required": ["id", "name", "createdAt", "ownerId"]
    },
    "Prompt": {
      "title": "Prompt",
      "description": "Represents a single AI prompt owned by a user.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "content": { "type": "string" },
        "category": { "type": "string", "enum": ["Video", "Imagen", "Textos", "Otros"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "projectId": { "type": "string" },
        "ownerId": { "type": "string" },
        "order": { "type": "integer", "description": "The position of the prompt in the list." }
      },
      "required": ["id", "title", "content", "category", "createdAt", "updatedAt", "ownerId"]
    },
    "Link": {
      "title": "Link",
      "description": "Represents a bookmarked URL within a project.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "url": { "type": "string", "format": "uri" },
        "projectId": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "category": { "type": "string", "enum": ["Video", "Imagen", "Textos", "Otros"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "ownerId": { "type": "string" }
      },
      "required": ["id", "url", "projectId", "createdAt", "ownerId"]
    }
  },
  "auth": {
    "providers": ["anonymous", "password"]
  },
  "firestore": {
    "/users/{userId}/projects/{projectId}": {
      "schema": { "$ref": "#/backend/entities/Project" },
      "description": "User-specific projects for prompt organization."
    },
    "/users/{userId}/prompts/{promptId}": {
      "schema": { "$ref": "#/backend/entities/Prompt" },
      "description": "User-specific AI prompts."
    },
    "/users/{userId}/links/{linkId}": {
      "schema": { "$ref": "#/backend/entities/Link" },
      "description": "User-specific links or bookmarks."
    }
  }
}