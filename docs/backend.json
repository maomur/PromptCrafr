{
  "entities": {
    "Project": {
      "title": "Project",
      "description": "Carpeta principal. Agrupa recursos sueltos y carpetas; la jerarquía no baja de ahí.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "description": {
          "type": [
            "string",
            "null"
          ],
          "description": "Texto libre opcional sobre para qué sirve."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "ownerId": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "name",
        "createdAt",
        "ownerId"
      ]
    },
    "Prompt": {
      "title": "Prompt",
      "description": "Represents a single AI prompt owned by a user.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "content": {
          "type": "string"
        },
        "category": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "Video",
            "Imagen",
            "Textos",
            "Otros",
            null
          ]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        },
        "projectId": {
          "type": [
            "string",
            "null"
          ],
          "description": "Proyecto al que pertenece, o null si está sin clasificar."
        },
        "ownerId": {
          "type": "string"
        },
        "order": {
          "type": "integer",
          "description": "Posición en la lista; un valor mayor aparece antes."
        },
        "folderId": {
          "type": [
            "string",
            "null"
          ],
          "description": "Carpeta dentro del proyecto, o null si está suelto en él."
        }
      },
      "required": [
        "id",
        "title",
        "content",
        "createdAt",
        "updatedAt",
        "ownerId",
        "order"
      ]
    },
    "Link": {
      "title": "Link",
      "description": "Represents a bookmarked URL within a project.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "url": {
          "type": "string",
          "format": "uri"
        },
        "projectId": {
          "type": [
            "string",
            "null"
          ],
          "description": "Proyecto al que pertenece, o null si está sin clasificar."
        },
        "title": {
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "category": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "Video",
            "Imagen",
            "Textos",
            "Otros",
            null
          ]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "ownerId": {
          "type": "string"
        },
        "order": {
          "type": "integer",
          "description": "Posición en la lista; un valor mayor aparece antes."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        },
        "folderId": {
          "type": [
            "string",
            "null"
          ],
          "description": "Carpeta dentro del proyecto, o null si está suelto en él."
        }
      },
      "required": [
        "id",
        "url",
        "createdAt",
        "ownerId",
        "order"
      ]
    },
    "Folder": {
      "title": "Folder",
      "description": "Subdivisión de una carpeta principal. La jerarquía admite hasta 3 niveles: proyecto › carpeta › carpeta.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "description": {
          "type": [
            "string",
            "null"
          ],
          "description": "Texto libre opcional sobre para qué sirve."
        },
        "projectId": {
          "type": "string",
          "description": "Proyecto raíz. Desnormalizado: siempre coincide con el del padre, para poder filtrar un proyecto entero sin recorrer el árbol."
        },
        "parentId": {
          "type": [
            "string",
            "null"
          ],
          "description": "Carpeta contenedora, o null si cuelga directamente del proyecto."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "ownerId": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "name",
        "projectId",
        "createdAt",
        "ownerId"
      ]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "/users/{userId}/projects/{projectId}": {
      "schema": {
        "$ref": "#/backend/entities/Project"
      },
      "description": "User-specific projects for prompt organization."
    },
    "/users/{userId}/folders/{folderId}": {
      "schema": {
        "$ref": "#/backend/entities/Folder"
      },
      "description": "Carpetas de los proyectos del usuario."
    },
    "/users/{userId}/prompts/{promptId}": {
      "schema": {
        "$ref": "#/backend/entities/Prompt"
      },
      "description": "User-specific AI prompts."
    },
    "/users/{userId}/links/{linkId}": {
      "schema": {
        "$ref": "#/backend/entities/Link"
      },
      "description": "User-specific links or bookmarks."
    }
  }
}
