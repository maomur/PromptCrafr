'use client';

/**
 * MobileDndPolyfill is no longer required as SortableJS handles 
 * touch events natively and correctly with the Force Fallback option.
 */
export default function MobileDndPolyfill() {
  return null;
}